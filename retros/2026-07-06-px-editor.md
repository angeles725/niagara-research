<!-- review-status: pending -->
# Retro — niagara-research · px-editor · 2026-07-06 · Research-SDD self-retrospective

> Run reviewed: px-editor B191-B196 (6 blocks), bootstrap→STOP same day, all gaps E1-E6 closed 6/6. Trigger:
> focus-completion. Method: fresh-context agent read `PROMPT-LOOP.md` + `METHODOLOGY.md` first, then the prior
> px-menu retro (dedupe), `RESEARCH-STATE-px-editor.md`, `git log --oneline -8`, the bootstrap commit diff, B194
> (line-by-line against its preserved sources), and cross-checked citations in B191/B194/B183 against the
> `docSource`/decompiled trees on disk. READ-ONLY on the kit — propose only.

## Proposed kit deltas

| # | Proposed change | Target (file · §/section) | Evidence | Type | Priority |
|---|---|---|---|---|---|
| 1 | Name the **`docSource` vs decompiled dual-tree** hazard as a distinct §5 case: some classes exist as GENUINE shipped SDK source (`organized/docSource/...`, javadoc intact) while sibling classes for the SAME gap exist only as decompiled output (`organized/<module>/vineflower/...`), with INDEPENDENT line numbering. A citation must be traceable to the ONE tree actually preserved for that class — never assume a block's other citations share the same tree. B194's RESEARCH-STATE row explicitly logs "Reconciliación líneas docSource vs decompilado" as a step the sweep had to do; B183 §183.6 hits the same fork the other way (`Parser.java` absent from docSource, present in vineflower). This is distinct from the existing beautified-temp rule (cosmetic transform of ONE file) and the obfuscated-bytecode rule (renamed symbols) — here two DIFFERENT physical trees compete for the same class name. | `METHODOLOGY.md §5` (new subsection, alongside beautified-temp/obfuscated-bytecode) | `RESEARCH-STATE-px-editor.md` iter-4 row; `niagara-mental-model-bloque194.md` header + `sources/SOURCES.md:37`; `niagara-mental-model-bloque183.md` §183.6. Re-verified live: `BPxMedia.java:28,57,66,130-135` all resolve correctly in the preserved `sources/decompiled/px-media/BPxMedia.java` (docSource copy) — the reconciliation the run reports was caught BEFORE commit, corpus is clean. `grep -rn "docSource\|vineflower" METHODOLOGY.md` → no hits pre-existing. | new | MEDIUM |
| 2 | Add an explicit **lighter bootstrap exception** to the "do NOT hand-guess... DELEGATE an audit sweep" rule for a new focus over a mature corpus (PROMPT-LOOP step e / METHODOLOGY §13): when the new focus is opened by the SAME driver immediately after closing a SIBLING focus on the SAME subsystem (shared domain knowledge just proven), hand-mapping N axes is acceptable IF each gap still passes e2's per-gap source-existence confirmation (a "Fuente confirmada" column filled at bootstrap, not left blank). This run's bootstrap commit (`7beeccd`) filled that column for all 6 gaps at bootstrap time and skipped a delegated audit-matrix sweep entirely — worked cleanly (6/6 closed, no wasted iteration) but is a literal deviation from the "do NOT hand-guess" wording as currently written, which does not carve out this case. | `METHODOLOGY.md §13` + `PROMPT-LOOP.md` bootstrap step e | `git show 7beeccd -- RESEARCH-STATE-px-editor.md` (bootstrap diff: all 6 gaps show a filled "Fuente confirmada" column, no audit-matrix artifact under `audits/`); px-menu retro closed 2026-07-06 immediately prior (shared domain). | new | MEDIUM |
| 3 | Add one sentence to §16/§8 distinguishing **reopen** (same axis, additive gaps, no re-bootstrap) from **new focus** (different axis, fresh `RESEARCH-STATE-<focus>.md` + fresh gap-prefix) specifically for the "user says 'keep going' right after a STOP" trigger — today this decision is derivable only by combining §8's reopen paragraph + §16 + the §b2 angle-declaration, with no single line naming the trigger itself. This run treated "keep documenting the PX editor" as a NEW FOCUS (new state file, new `E`-prefix) rather than a reopen of px-menu, which reads as the correct call (px-menu's axis was the menu vertical slice; px-editor's is the wider tool/widget/binding/media/theming/animation surface) but required the fresh-context retro to reconstruct WHY from three separate sections. | `METHODOLOGY.md §16` (one-sentence cross-reference) | `RESEARCH-STATE-px-editor.md` "Ángulo declarado" section explicitly contrasts itself against px-menu's scope; bootstrap commit `7beeccd` message "tras cerrar px-menu". | refinement | LOW |

- **#1** — Cheap (one paragraph); the run's own bookkeeping proves the hazard is real and already had to be manually reconciled once — naming it turns "reconciliación" from an ad hoc save into a checked step (e.g. "confirm which tree before citing, not after").
- **#2** — Without this, the kit's own text technically calls this run's bootstrap a rule violation despite it working correctly with no quality loss (source-existence was still honored). Either tighten the rule's wording or add the carve-out — leaving the contradiction undocumented lets future runs cite this exact commit as precedent without knowing if it was sanctioned.
- **#3** — Low cost, mostly editorial; the decision was made correctly here so this is polish, not a fix for an actual mistake.

## Already covered (dedupe)

- **Model-tier persisted on disk per iteration** → already covered (px-menu retro delta, now standing kit practice): all 6 rows in `RESEARCH-STATE-px-editor.md`'s iteration table have a filled "Delegado? · tier" column.
- **Multi-focus corpus conventions** (`RESEARCH-STATE-<focus>.md`, `FOCUSES.md`, shared global block numbering, engram topic key `research/<target>/<focus>/...`) → already covered by `METHODOLOGY.md §16`; ran with no friction (B191 starts exactly at the next global number after px-menu's B190).
- **Focus-closing declaration + §18 retro trigger at focus completion** → already covered by `METHODOLOGY.md §8`/`§18`; B196 explicitly closes the focus and RESEARCH-STATE records the STOP with the 0/0/0 classification.
- **DESIGN/APPLIED high-`[INFER]` ratio not an exhaustion signal** and **gap closure taxonomy** → already covered by §8/§11 (not specifically exercised at a notable ratio this run, but no violation either).
- **Cross-target sourcing pattern** (px-menu retro delta #1) — this run did not need it (no cross-target citation); not re-tested, not re-proposed.

## Metrics

- **Blocks reviewed**: 6 (B191-B196), 3 read in full (B191, B194, B183 for cross-check), rest via RESEARCH-STATE/commit log.
- **§14-style corrections**: 0 observed in this run.
- **Deltas proposed (new)**: 2 (MEDIUM) + 1 (LOW refinement) = 3.  **Already-covered lessons**: 5.

## Honest verdict

This run is a clean, fast bootstrap→STOP (same day, 6/6 gaps, no corrections) that mostly exercised
already-covered machinery correctly. Two things are genuinely worth the kit's attention: the `docSource`-
vs-decompiled dual-tree citation hazard (#1) is real and already had to be manually caught once (per the
run's own bookkeeping), even though the final committed citations verify clean — naming it prevents relying
on luck next time. The hand-mapped bootstrap (#2) is a more pointed finding: it is a literal deviation from
the kit's current "do NOT hand-guess, delegate an audit sweep" wording, and it worked fine here specifically
because the driver had just-proven domain knowledge from a closed sibling focus — the kit should either
sanction this shortcut explicitly or flag it as a deviation, not leave it ambiguous. #3 is minor polish, not
a real gap — the reopen-vs-new-focus call was made correctly, just not traceable to one line of doctrine.
