<!-- review-status: pending -->
# Retro — niagara-research · px-menu · 2026-07-06 · Research-SDD self-retrospective

> Run reviewed: px-menu B179-B190 (12 blocks), bootstrap→STOP (12/12 gaps, focus closed). Trigger:
> focus-completion + corpus-level checkpoint. Method: fresh-context agent read `PROMPT-LOOP.md` +
> `METHODOLOGY.md` first, then `RESEARCH-STATE-px-menu.md`, the 16-commit git log, B179/B182/B184/B189,
> `sources/SOURCES.md`, and ran `toolbelt/verify-sources.sh` live. READ-ONLY on the kit — propose only.

## Proposed kit deltas

| # | Proposed change | Target | Evidence | Type | Priority |
|---|---|---|---|---|---|
| 1 | Document a **cross-target citation/preservation pattern**: when a gap's authoritative evidence lives in a *different registered target* (not a sibling focus of the same target — §16 covers that), preserve a copy into the citing target's own `sources/` and register it in `SOURCES.md` noting the origin target's name/path, exactly as B180 did pulling `docGraphics.txt` from `niagara-help` (target #3) into `niagara-research/sources/text-extracts/`. Today this is handled only by an ad-hoc note in `RESEARCH-STATE-px-menu.md` ("Fuentes cross-target"), invented per-run; §5/§16 have no rule for it. | `METHODOLOGY.md §5` (new subsection) or `§16` | `RESEARCH-STATE-px-menu.md` "Fuentes cross-target" note; `sources/SOURCES.md` row `text-extracts/docGraphics-px-editor.md` (origin `niagara-help/docs-text/...`, cited B180); `TARGETS.md` row #3 `niagara-help`. `grep -rn "cross-target\|cross-corpus"` over METHODOLOGY/PROMPT-LOOP/TARGETS returns nothing pre-existing. | new | HIGH |
| 2 | Codify **directory-style grouped rows** in `SOURCES.md` as the safe convention when one gap's evidence spans several small sibling files (e.g. 6 pane classes for one gap): one row per *directory* (`decompiled/bajaui-wb-layout/`, description lists the class names), not one row per file and NOT a compound filename (`A.java + B.java`). This run used the directory form for B182/B183/B184 and `verify-sources.sh` LEVEL-4 (basename cross-check of the "Cited by" column against block text) passed cleanly (`exit 0`, 25/25 cross-checked). A compound `"A.java + B.java"` filename would instead break that basename match — worth naming the safe pattern explicitly rather than leaving row-granularity undocumented. | `METHODOLOGY.md §5` | `sources/SOURCES.md` rows for `decompiled/bajaui-wb-layout/`, `decompiled/gx-rt/`, `decompiled/converters-rt/`; live re-run of `toolbelt/verify-sources.sh /home/cristian/niagara-research` → `exit 0`, no FABRICATED-CITE/WARN | new | MEDIUM |
| 3 | Add a caveat to the §11 token-check step: a plain single-line `grep` can produce a **false negative** on a load-bearing token whose literal is line-wrapped or built via string concatenation in the decompiled source — that miss is a grep limitation, not proof the citation is wrong, and should trigger a whitespace-normalized/multi-line re-check before downgrading a marker. Reported for the sonnet-delegated sweeps in this run (B181-B184); the raw self-verify transcript isn't persisted in committed blocks (only the final marker tally is, per current §11 design), so this evidence is the run's own account, not independently re-derivable from the corpus as committed. | `METHODOLOGY.md §11` (token check bullet) | Run account for B181-B184 delegated sonnet sweeps; not independently verifiable from committed block text (self-verify narrative is not persisted per §11's current scope) | new (evidence partial) | MEDIUM |

- **#1** — Without a named pattern, every cross-target gap reinvents "how do I safely cite a sibling install's docs" from scratch; cheap to add (a paragraph), and B180 already proves the working recipe.
- **#2** — Cheap (one paragraph); prevents a future operator from picking the "A.java + B.java" naming that would silently break `verify-sources.sh`'s own cross-check, since the linter has no rule stated anywhere about row granularity.
- **#3** — Prevents an unnecessary marker downgrade on a real citation; low cost, but evidence is soft (not visible in committed corpus), so keep priority at MEDIUM not HIGH pending a clearer instance.

## Already covered (dedupe)

- **DESIGN/APPLIED block with a high `[INFER]`/`[CERT]` ratio is expected/healthy, not an exhaustion signal** → already covered by `METHODOLOGY.md §11`/`§8`. B189 (ratio 0.69) self-labels exactly this, citing §11 by number.
- **Gap closure by remittance, citing prior coverage + "no new substance"** → already covered by `METHODOLOGY.md §8`. G10/B187 applied it correctly (cites `bloque35:3` as remittance, while adding genuinely new investigation on `BOrd.java`).
- **Model-tier persisted on disk per iteration (not just in the ephemeral report)** → already covered (this was itself a delta from the 2026-07-02 nmodsreflow retro, now applied): `RESEARCH-STATE-px-menu.md`'s Iteration history table has a "Delegado? · tier" column filled for all 12 rows.
- **Cross-block self-correction mechanism (§14)** → already covered by `METHODOLOGY.md §14`. B182 §182.4's "BBorderPane ≠ 5-region pane" note uses the mechanism, though no *prior committed block* actually asserted the wrong fact (it reads as catching a same-run assumption before publication) — a softer case than classic §14, not itself a gap worth a new rule.
- **Multi-focus corpus conventions** (`RESEARCH-STATE-<focus>.md`, `FOCUSES.md`, shared global block numbering) → already covered by `METHODOLOGY.md §16`; px-menu's bootstrap/handoff ran with no friction.

## Metrics

- **Blocks reviewed**: 12 (B179-B190), 4 read in full (B179, B182, B184, B189), rest via RESEARCH-STATE/commit log.
- **§14-style corrections**: 1 (B182 §182.4, soft case — see above).
- **`verify-sources.sh` result**: exit 0 (29 `[CERT-doc]`, 60 `[CERT-a]`, 21 `[CERT-web]` blocks; 26 registry rows; 25 citations cross-checked; 0 missing files).
- **Deltas proposed (new)**: 3.  **Already-covered lessons**: 5.

## Honest verdict

This run is a clean execution of the kit's existing machinery (§8/§11/§14/§16 all fired correctly and are
cited by number inside the blocks themselves) — most of what it "did well" needed no new rule. The genuinely
new material is narrower than the task briefing suggested: the compound-filename FABRICATED-CITE failure
mode (candidate area c) did **not** actually occur — the run avoided it via directory-style grouping, which
is itself worth codifying (#2) precisely because it was an unwritten, easy-to-miss convention. The clearest
real gap is cross-target sourcing (#1), which the operator had to improvise with an ad-hoc RESEARCH-STATE
note. #3 is included on the task's account but its evidence could not be independently confirmed from the
committed corpus, so its priority is capped at MEDIUM rather than HIGH.
