# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
*.swp
*.swo
.idea/
.vscode/

# Claude Code / runtime
.drafts/

# Orchestrator-local artifacts (not corpus) — research-sdd METHODOLOGY §15
.atl/
.claude/

# Python bytecode
__pycache__/
*.pyc

# Credentials — NEVER commit. Rotating the password is the only real fix once
# a secret reaches git history, so keep it out in the first place.
.secrets
.secrets.*
*.env
.env
.env.*
credentials
credentials.*
*.pem
*.key
*.p12
*.jks
organized/
niagara-help/
module-navigator/

# Full JACE-8000 microSD raw image + extracted payloads (secret-bearing: config.bog, keyrings, shadow, factory creds). Kept in-repo for local work, NEVER committed.
local-sd-image/
