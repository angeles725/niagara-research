# Campaign 9 — evidence map per slice (propose/apply input)

Author: investigador1, 2026-09-06. One row per C9 slice: **citation · the one sentence apply must build · seam · schema-risk
expectation**. Anchors: client tip **a109249** (`niagara-panccadia-leon` origin/main), kit main **c28b05e**, tunnel main
**9acb47c** (not clonable here — viewer echo), C9 artifacts `openspec/changes/build-n4-module-campaign9/{explore f3e86f6,
proposal ba3432c, spec, design}`. Every row tokened; `[CERT via viewer echo]` = a second party's verbatim read I could not repeat.

> **Tree-anchoring warning for apply (found this session, proven):** the C9 **design** cites `CompressorControl.java` and
> `BEvaporatorUnit.java` at the OLD pre-fix tree **`4f5f1c7`** (the second local clone), while the **proposal** cites
> `a109249`. Every D1/D8 line number must be re-anchored to a109249 before apply — see
> `2026-09-06-c9-s20-rotation-design-evidence.md` §1 for the proven side-by-side. B824's cites are already correct at a109249.

| Slice | Citation (block / RED branch+tip) | The one sentence apply must build | Seam | Schema-risk expectation |
|---|---|---|---|---|
| **S20** rotation (R1, PR1) | proposal §1 :17; client `CompressorControl.java` @a109249 — stage-up picks least-hours-off `:226`, stage-down drops most-hours-on `:238`, `cmdSince[]` `:71`, `lastStageMs` `:72`, `stageReady` `:221` `[CERT]`; RED `qa/c9-comppan-rotation` **to author** (ROT1-ROT10, spec :48-61) | Add two ADDITIVE slots (`rotationInterval` BRelTime, `rotationMode` new non-linked enum) on `BCompressorControl` and a two-cycle make-before-break state machine in `CompressorControl.step`: **completion (2b) between the target clamp `:217` and the stage move `:219`; arm (3b) between the stage-move brace `:246` and the HOA loop `:248`**; `rotationInterval = 0` is a pure no-op. `[ev: design D1]` `[ev: client :217-248]` | pure `CompressorControl.step(now, demandCount, …)` + two cfg lines beside `BCompressorControl.java:1907-1909` (`cfg.minOnMs/minOffMs/stageDelayMs`) `[CERT]` | **SAFE** — additive slots, new enum has no cross-module link (B828 §828.7-safe); `schema-risk.sh` SAFE against the real bog is a PR gate; rollback-after-deploy = `rotationInterval = 0`, not a jar downgrade (proposal §9) `[ev: corpus B828 §828.7]` |
| **S12-A** write-server (R4/R5, PR4/PR5) | RED `qa/c9-s12-write-server` **`e7e6615`** (supersedes 24adcba; REBASED on tunnel main 9acb47c — RK3 done; c217b0a RED → 55d6797 re-pin S12A-4/6 on the extended `change_log` → e7e6615 adds S12A-8 failed-write audit row + S12A-9 spool replay idempotency) "test(pipeline): RED — write-server config-login step-up + audited setpoint write (campaign 9 S12 surface A)"; adds `instalacion/pipeline/test/write-server.config-login.test.mjs` (+156); base **e4b42b0** confirmed; cases S12A-1 no-token→403 · S12A-2 wrong-pw→401 · S12A-3 login→200+token · S12A-4 write+token→200 + exactly one audit row (8 fields) · S12A-5 logout→403 + stale→403 · S12A-6 audit-append failure → write still 200 + error row · S12A-7 setpoint PUT `…/setpoint/value` bare `<real>`; named mutation: drop the token check → S12A-1/S12A-5 flip `[CERT — read on the local tunnel clone /home/cristian/tunnel/clientes/Leon-Guanajuato/Pancaddia: tip e7e6615, `buildServer` absent from write-server.mjs]` | Build `buildServer(cfg, deps)` (returns the handler without binding a port; `main()` binds), `POST /config/login` minting a **server-held** token bound to `(email, purpose)` with absolute TTL + sliding inactivity, `POST /config/logout` revoking it, token-gated `/write` + `/alarms/ack`, pre-write GET for `old`, exactly ONE `change_log` row per write, failure spool. `[ev: design D6/D7]` | `buildServer(cfg, deps)` with injected `{supabase, station, clock, spool}` — **NOT present on 24adcba; the RED expects apply to add it** `[CERT via viewer echo]` | n/a for Niagara. **DIVERGENCE:** tunnel main **9acb47c** already inserts a best-effort `public.change_log` row per `/write` (JWT-only, no step-up/logout/result/session) — rebase 24adcba onto 9acb47c BEFORE apply (RK3), keep the existing insert passing; Postgres migration **additive-only** pin (no drop/retype) `[ev: proposal §5]` `[ev: proposal RK3]` |
| **S12-B** servlet (R6, PR6) | RED `qa/c9-s12-servlet` **`4c18837`** (rebased on a109249): compile-RED on `DashboardWriteGuards.evaluate`; 7 pins guard1_noXhr_302 · guard2_noRemoteUser_401 · guard3_noOperatorWrite_403_failClosed · guard4_invalidValue_400_noSilentZero_noAudit · guard4b_emptyAndNan_400 · guard5_ordTraversal_400 · success_200_appendsExactlyOneAuditEntry `[CERT]` (verified on the local clone) | Extract a static `DashboardWriteGuards.evaluate(…)` returning `Verdict{status, errorJson, parsed}` that `handleSetpointWrite` calls once; guard 4 (a numeric-value 400) ALREADY EXISTS at `BDashboardServlet.java:274-288` (PR#7, verified a109249) — the RED's guard4/guard4b are REGRESSION protection against the latent `parseDouble`→0.0 at `:403-407`, unreachable at a109249 only because that guard runs first (design D8a, corrected) — BUT the DEPLOYED DashboardPan is **2.0**, which has no guard: the silent zero IS live on the station until the pending 2.0.7/2.0.3/2.1.1 deploy lands (RAR read a34f9bdd2); pass the REAL request-user Context to `parent.set(prop, toSet, cx)` so the write is Niagara-audited (B829-G2). `[ev: design D8]` `[ev: corpus B829]` | `evaluate(xhr, user, operatorWrite, ord, value, AuditSink)` — pure inputs + a sink `[CERT — test source]` | **SAFE** (no slot touched; real-Context is schema-neutral). **Re-anchor D8's `BDashboardServlet.java` cites**: D8 says `parent.set :274`, `coerceValue :344-347`, `parseDouble :386-391`; the S12 plan read the same file at fbe9009 as `parent.set :291`, `coerceValue :357`, `handleSetpointWrite :195`, `appendAudit :312` — same drift pattern as D1 `[INFER — verify at a109249 before apply]` `[ev: S12 plan 1ecdf437c]` |
| **S7** demand-in-scope lint (R2, PR2) | RED `qa/c9-demand-in-scope` **`2916954`**: DS1-DS7 + DS-smoke; DS2 = `fail_demand_removed_mutant`; smoke on real `CompressorControl.step`; script ABSENT on branch (true RED) `[CERT]`; B820 `16b635f0f` | Build `toolbelt/lint-demand-in-scope.sh` (standalone) that WARNs a method reading a process variable (pressure) with no demand symbol in scope (param or enclosing field), `--strict` → FAIL, no src-dir → exit 3, D9b dot-dir prune; K19 routing in BUILD-LOOP §5 + SKILL. `[ev: corpus B820]` | shared lint skeleton (design D2/D3/D10): `<check> WARN|FAIL <file>:<line> <detail>`, exits 0/1/3 | n/a (kit) |
| **S18-lint** silent-protection (R3, PR3) | RED `qa/c9-silent-protection` **`e38e503`**: SP1-SP8 + SP-smoke; SP8 = `mutation_removing_reason_slot_makes_flag_appear`; smoke flags **CP-1** + **CR-3**, NOT CP-2 `[CERT]`; B824 `b5060f60b` — CP-1 = inline `Math.min(target, onCount-1)` on `suction < suctionLowLimit` with no named field/slot (`CompressorControl.java:215` — **still :215 at a109249** `[CERT]`); CR-3 = `freezeTripped` private field `BEvaporatorUnit.java:1287` feeding `valveInhibited()` `:1102-1106` (**still correct at a109249** `[CERT]`) | Build `toolbelt/lint-silent-protection.sh`: a protection trip whose only surface is a private field / bare SUMMARY slot with no `BAlarmSourceExt` → WARN; effect-slot exemption, pure-model→adapter field→slot follow, reason-slot allowlist; `--strict`, exit 3, D9b. `[ev: corpus B824]` | shared lint skeleton | n/a (kit). Note: the design's `freezeTripped :1173 / valveInhibited :1047-1052` are the **4f5f1c7** numbers — B824 is right, the design drifted |
| **S19** ext-writable-shape (R10, PR10) | RED `qa/c9-ext-writable-shape` **`269be48` (re-issued from 3726722: 28feb42 script path `lint-ext-writable-shape.sh`, 717d585 real-tree root via `C9_CLIENT_ROOT`, 3387c58 EW11 no-sources→exit 3 (K20), 269be48 EW10 exact a109249 contract = 1 WARN `BRoomPanel.setpoint` on DashboardPan-rt, 0 on the other three roots)**: EW1-EW11; script ABSENT (true RED) `[CERT — tip re-read 2026-09-06]`; B823 + retro `obix-statusnumeric-wrapped-put` | Build `toolbelt/lint-ext-writable-shape.sh`: a bare complex `OPERATOR` property (`BStatusNumeric/Boolean/Enum`) with no writing `@NiagaraAction` → WARN naming the child `…/value` leaf as the preferred external write form; plain double/boolean/reltime or complex-with-action → clean. `[ev: corpus B823]` `[ev: corpus B826]` | shared lint skeleton | n/a (kit) |
| **S18/S13** alarm PoC (R8/R9, PR8/PR9) | B827 `ff0ce3f5b` §827.3/§827.4/§827.6 (copy-ready CR-3 + CP-1 sketches); B821 §821.4 (tier-1 unused, ZERO `BAlarmSourceExt|BAlarmRecord|BAlarmService` in the RT modules) `[CERT]`; REDs **to author** `qa/c9-alarm-cr3` (CRA1-6), `qa/c9-alarm-cp1` (CPB1-7) | **Pattern A (CR-3 freeze, declarative):** add a child `BBooleanPoint` driven from the trip + a `BAlarmSourceExt` with `BBooleanChangeOfStateAlgorithm`. **Pattern B (CP-1 low-suction, programmatic):** `BCompressorControl implements BIAlarmSource` (`ackAlarm` as `@NiagaraAction`), `AlarmSupport(this, "defaultAlarmClass")` in `started()`, `newOffnormalAlarm` ONLY on the normal→offnormal EDGE with `toNormal` on recovery + `started()` re-seed (`wasOffnormal[]`, B827-G1). `[ev: corpus B827]` | the trip site → child point (A) / `support` call (B); `sourceState = offnormal` is what the DashboardPan bql already selects (`BDashboardServlet.java:502`, B827) | **SAFE** — additive child point / additive action + transient `AlarmSupport`; no retype/remove (B795). Live gate **B827-G2** (routed alarm reaches console + panel) closes the PoC, never blocks the PR `[ev: corpus B827]` |
| **S5-cont** write-path matrix (R11, PR11) | RED `qa/c8-write-path` **`5e357d1`** `[CERT]`; design D11a/D11b; retro `campaign8-write-path` measured uncovered set | Author the matrix rows **in the CLIENT repo** (`<client-root>/docs/write-path-matrix.md` — `build-n4-module-kit/docs/` does not exist, D11a), sized by the MEASURED uncovered set (27 distinct slots + PR1's `rotationInterval`/`rotationMode`), i.e. `W14…W40+`, not a fixed nine (D11b); each row names a test present in `srcTest/`. `[ev: design D11]` | `lint-write-path.sh` reads the matrix; delete one row → that slot FAILs again (PR matrix mutation) | n/a (docs + lint) |
| **K22/§5** doctrine fold (R12, PR12) | proposal §6 + design D12; C8 close retro lesson 11 (`campaign8-close-process-meta-lessons` Δ11) `[CERT]` | Fold: BUILD-LOOP §5 the ONE module-root/profile convention (root = the dir containing the profiles; every lint ITERATES profiles; no sources/no matrix → exit 3, never silent 0); METHODOLOGY **K22**; `types/logic-authoring.md` "Slot types for externally written values"; `types/logic.md` alarm patterns A/B; `types/dashboard.md` slot-type one-liner; every new script routed in BUILD-LOOP + SKILL (K19, `kit-links.bats`). `[ev: proposal §6]` | doc | n/a; gate = `kit-links.bats` green, one `[ev:]` per row |

## Cross-slice dependencies (apply order)
1. **W1** S20 (PR1) → S7 (PR2) → S18-lint (PR3): PR1 needs its RED authored first; PR2/PR3 REDs exist and are true test-first.
2. **W2** S12-A (PR4→PR5) needs the **9acb47c rebase** first; S12-B (PR6) is independent of the tunnel but shares the
   `change_log` schema through the R7 mirror (PR7) — mirror ships flag-gated OFF; live enablement = B829-live gate.
3. **W3** alarm PoC (PR8/PR9) needs its two REDs; S19 (PR10) RED exists; S5-cont (PR11) depends on PR1 for the two new rows;
   K22/§5 (PR12) folds last; close (PR13).

## Verify table
| # | Claim | Marker | Evidence |
|---|---|---|---|
| 1 | Design D1 cites = 4f5f1c7; proposal cites = a109249 | [CERT] | `git show 4f5f1c7:…CompressorControl.java` `:230` call / `:304` def / `:233` brace; `a109249` `:238` / `:365` / `:246` |
| 2 | B824's CP-1 `:215` and CR-3 `:1287,:1106` hold at a109249 | [CERT] | grep at a109249 this session |
| 3 | S12-A tip/base/cases/seam-absent | [CERT via viewer echo] | lead relay 2026-09-06; not re-read here |
| 4 | S12-B compile-RED, 7 pins | [CERT] | `git show origin/qa/c9-s12-servlet:…DashboardWriteGuardsTest.java` |
| 5 | S7/S18-lint/S19 true REDs (script absent, bats present) | [CERT] | `git ls-tree` on the three origin branches |
| 6 | D8 BDashboardServlet cites drifted | [INFER] | S12-plan fbe9009 numbers ≠ D8 numbers; same tree pattern as D1; verify at a109249 |
