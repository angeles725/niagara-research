# B696 evidence — JACE_UMBRELLA station keystores (structural/public cert metadata; NO private keys)
# Source: SD P2 via qnx6read.py. Cert subjects are PUBLIC; private key material NEVER extracted.

## keystore.jceks (4476 B, magic CE CE CE CE = JCEKS):
  alias 'default' = the station TLS keypair (matches config foxsCert/httpsCert='default', B685)
  cert subject tokens (public): Niagara4 / ForRecoveryPurposes / tridium / Tridium
  => the FACTORY default self-signed TLS cert (ForRecoveryPurposes) — not a CA-signed cert (confirms B398/B468 on disk)

## cacerts.jceks (32 B, magic CE CE CE CE): EMPTY JCEKS (0 entries) — no station-added trust anchors
## untrusted.jceks (32 B, magic CE CE CE CE): EMPTY JCEKS (0 entries) — no explicit untrusted entries

## signing/signers (33021 B, magic 00 2E 5B 61 = non-JCEKS): module-signing signer store
  (module-signing trust model = REMITTANCE signing-pki B392-B396; DAR4 records deployment presence only)

# secret-scan: no private-key/keystore-password bytes; only public cert subject strings.
