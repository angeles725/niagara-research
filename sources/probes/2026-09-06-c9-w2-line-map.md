# C9 W2 line map — tunnel `write-server.mjs` @ 9acb47c · client `BDashboardServlet.java` / `DashboardDispatch.java` / `DashboardRbacHelper.java` @ a109249

investigador1, 2026-09-06. file:line only. Trees: tunnel `/home/cristian/tunnel/clientes/Leon-Guanajuato/Pancaddia` @ 9acb47c; client worktree `Cliente/Leon-Guanjuato-worktrees/main-a109249`. `[CERT — grep/sed this session]`

## PR4/PR5 — `instalacion/pipeline/write-server.mjs` @ 9acb47c (309 lines)
| Site | Line(s) |
|---|---|
| `HERE = dirname(fileURLToPath(import.meta.url))` (only import.meta use) | :21 |
| `loadConfig()` | :24 |
| `FACADE_PATH` default `/config/Services/DashboardService` | :45 |
| `rateLimited(cfg, ip)` | :65 |
| `const WRITABLE = {` … `}` (setpoint :82 … intercambiadorMode :93, coolOnSensorFault :94 …) | :81-99 |
| `obixBody(type, value)` | :100 |
| `obix(cfg, method, absPath, body)` | :132 |
| `auditChange(cfg, row)` → `fetch(SUPABASE_URL + '/rest/v1/change_log')`; warn-only on !ok / catch | :154-168 (:157 fetch, :167 !ok, :168 catch) |
| `obixReadVal(xml)` | :171 |
| `getJwks(cfg)` / `verifyJwt(cfg, token)` | :179 / :186 |
| `send(res, code, obj, cfg)` | :209 |
| `readBody(req)` | :224 |
| `async function main()` — **seam site**: split into `buildServer(cfg, deps)` + `main()` | :231 |
| `http.createServer(async (req, res) => {` | :233 |
| OPTIONS 204 / GET `/health` 200 / non-POST 405 | :238 / :239 / :240 |
| `ip` = `cf-connecting-ip` ‖ `socket.remoteAddress` | :243 |
| rate limit 429 | :244 |
| `user = await verifyJwt(cfg, token)` / 401 `auth` | :250 / :251 |
| body parse 400 `json` | :254 |
| **`/write` handler** | :256 |
| ord regex `^Cuarto([1-5])/(.+)$` / 400 `ord` | :259 |
| `type = WRITABLE[m[2]]` / 400 `ord no escribible` | :260 / :261 |
| `xml = obixBody(type, value)` / 400 `value` | :263 |
| **pre-write GET old** (`obixReadVal(gr.body)`, swallowed on error) | :268 |
| **oBIX PUT** `${FACADE_PATH}/${putOrd}` | :269 |
| `auditChange(cfg, { … })` row (user_email/user_id/room/slot/label/old_value/new_value/area/ok) | :272-279 (:279 `new_value: obixReadVal(xml)`) |
| 200 `{ok, ord, value, by}` | :283 |
| 403 `permiso` (oBIX 403 / PermissionErr) / 502 `jace` / 400 `obix` | :285 / :286 / :287 |
| **`/alarms/ack` handler** / uuid 400 / oBIX POST `/alarm/${uuid}/ack` / 200 / 502 / 400 | :290 / :292 / :294 / :296 / :297 / :298 |
| 404 `ruta` / catch-all 500 `server` | :301 / :303 |
| `server.listen(cfg.WRITE_PORT, '127.0.0.1', …)` | :306 |
| **`main().catch(...)` — unconditional auto-run, NO import.meta guard** (D6: gate with `import.meta.url === pathToFileURL(process.argv[1]).href`) | :309 |

## PR6 / R14 — `Dashboard/DashboardPan/DashboardPan-ux/src/com/angeles/DashboardPan/ux/BDashboardServlet.java` @ a109249 (612 lines)
| Site | Line(s) |
|---|---|
| `doGet(WebOp op)` | :91 |
| `doPost(WebOp op)` / `DashboardDispatch.route("POST", …)` / `instanceof SetpointWrite` → `handleSetpointWrite(req, resp)` / `Redirect` → `sendRedirect` / `NotFound` / catch | :132 / :143 / :145-147 / :149-151 / :153 / :177 |
| **`handleSetpointWrite(req, resp)`** | :195 |
| guard 2/3 — `DashboardRbacHelper.checkCanWrite(req, resp)` (401 / 403) | :198 |
| guard 5a — `relOrd` null/empty → 400 | :214-216 |
| guard 5b — traversal (`..`, `\`, NUL) → 400 | :223-225 |
| guard 5c — no `/` or trailing `/` → 400 | :232-234 |
| `BOrd.make(parentOrd).get(this, null)` resolve | :247 (catch :250) |
| guard 5d — `parent == null` → 400 / guard 5e — `prop == null` → 400 | :254-256 / :263-265 |
| **guard 4 — numeric/reltime value** (`JsonUtil.parseFiniteDouble(value).isPresent()` else 400 `invalid value`) | :274-288 (:281 test, :283 400) |
| `toSet = coerceValue(current, value)` | :290 |
| **`parent.set(prop, toSet, null)`** — the null-Context write (D8b/R14: → user-bearing Context) | :291 |
| `remoteUser = req.getRemoteUser()` | :293 |
| `svc.appendAudit(JsonUtil.buildAuditEntry(…))` (fire-and-forget) / `catch (Exception auditEx)` | :312 / :316 |
| outer `catch (Exception e)` | :321 |
| `coerceValue(current, rawValue)` — BStatusNumeric / BDouble / BRelTime / enum | :357 (:363 / :367 / :371 / :388-389) |
| `parseDouble(s)` — returns 0.0 on null / NFE (latent silent zero) | :403-407 |
| alarms handler: `req.getRemoteUser()` / 401 / bql `sourceState='offnormal' or 'fault'` / `sourceState` read | :474 / :477 / :502-503 / :534 |
| 404 / 405 | :596 / :605 |

## `…/ux/DashboardDispatch.java` @ a109249 (186 lines)
| Site | Line(s) |
|---|---|
| `RouteAction.Equipment` / `Alarms` / **`SetpointWrite`** / `StaticResource` / `Redirect` / `NotFound` / `MethodNotAllowed` | :46-48 / :53-55 / :60-62 / :67 / :74 / :81-83 / :88-90 |
| `static RouteAction route(String method, …)` | :108 |
| POST branch: traversal 404 / `/api/` / **XHR guard** (`X-Requested-With`) / `/api/setpoint` → `SetpointWrite` / unknown `/api/*` 404 / non-api POST 405 | :117-135 (:120 / :121 / :123 / :128-130 / :133 / :135) |
| GET branch: traversal / XHR guard / `/api/equipment` / `/api/alarms` / 404 | :139 / :142-144 / :149-151 / :153-155 / :158 |
| R14 insertion: `"/api/config/login"` + `"/api/config/logout"` cases beside :128-130 inside the XHR-guarded POST `/api/` block; singletons `ConfigLogin`/`ConfigLogout` beside :60-62 (CLW1) | — |

## `…/ux/DashboardRbacHelper.java` @ a109249
| Site | Line(s) |
|---|---|
| `static boolean checkCanWrite(req, resp)` | :33 |
| `remoteUser = req.getRemoteUser()` / null → 401 | :36 / :41 |
| `unescapeUsername(remoteUser)` (call / def) | :51 / :68 |
| `resolveOperatorWrite(lookupUser)` (call / def) / 403 | :52 / :78 / :55-56 |
| `userService.getUser(username)` / `user.getPermissions((Context) user)` / `.has(OPERATOR_WRITE)` | :90 / :96-97 / :98 |
| R14: guard 3 evaluates the CONFIG-SESSION user when present (d2857d1) → this helper gains a username-parameter entry point; kiosk may be viewer-only | — |
