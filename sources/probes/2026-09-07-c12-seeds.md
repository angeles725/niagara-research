# C12 seeds — evidence-anchored, from the C11 apply/second-read lane

investigador1, 2026-09-07. Consolidated so C12's explore starts from evidence, not memory. All source anchors at kit
**07d2fc7** (C11 PR1-PR4 merged; PR5 close pending). Each seed carries an anchor and a marker. `[ev: kit 07d2fc7; reproduced runs this session]`

## Open seeds

### S1 — B832-G3: the shared parser's Case-B `@`-stop is a latent FALSE NEGATIVE on a parameter-annotation continuation
`toolbelt/lib/method-boundary.sh:84` (`if (substr(lk_t, 1, 1) == "@") break`). A method whose signature spans lines with a
**parameter annotation** on the continuation (`void m(\n  @Ann x)\n{`) has a brace-only `{` whose backward scan hits the
`@Ann x)` line first → the `@`-stop breaks → the method is never named → its body (field-flag + `Clock.schedule`, a trip,
a do-body) is invisible to every consuming lint. REPRODUCED on lint-timers at merged main 127f25a: **0 companion-flag WITH
the `@`-stop (FN), 1 WITHOUT**. Absent from the ff1b659 client corpus (0 such shapes in 42 `.java`) — that is why C11
kept it as defensive. **C12**: replace the blanket `@`-stop with a rule that distinguishes a class/field-level annotation
(stop) from a parameter-annotation continuation of a signature (do not stop); pin with a `void m(\n @Ann x)\n{` fixture.
`[CERT]` `[ev: method-boundary.sh:84 @ 07d2fc7; B832-G3; reproduced @ 127f25a]`

### S2 — B832-G4: the Case-A keyword exclusion is reachable inside a non-entered instance/static initializer
`toolbelt/lib/method-boundary.sh:74` (Case-A `mname ~ /^(if|for|…)$/`) and `:89` (Case-B `class|interface|enum`). The
`!in_m` gate blocks nested control blocks inside a method, but an instance/static **initializer** `{` opens depth ≥ 2 and
is never named a method (no identifier), so a `for`/`if` inside it IS a live `!in_m` candidate open that I3 rejects.
REPRODUCED: a `for` inside an instance initializer flips **0 (with I3) → 1 (drop I3)** on lint-timers. Absent from the
client corpus. **C12**: a pin for the initializer shape (decides whether the keyword exclusion stays or the initializer
gets its own handling). Corrects the "I2/I3 are redundant like the PR4 absolutise" framing to "defensive on the corpus,
but each with a reachable shape". `[CERT]` `[ev: method-boundary.sh:74/:89 @ 07d2fc7; B832-G4; reproduced @ 127f25a]`

### S3 — `toolbelt/sweep-markers.sh`: a reusable conflict-marker sweep (promote the C11 point-check to a routed lint)
C11 added `CLOSE-no-conflict-markers` to `c11-close.bats` + the per-PR verify after I found real unresolved conflict
markers in a C8 archive doc (`openspec/changes/archive/2026-09-05-build-n4-module-campaign8/apply-progress.md:255/330/378`,
latent since campaign 8; now resolved). That is a bats-level point check. **C12**: a reusable `toolbelt/sweep-markers.sh`
(grep `^<<<<<<< ` / `^>>>>>>> ` / a true `^=======$` conflict middle, excluding markdown rules) routed in BUILD-LOOP + SKILL,
exit 0 clean / 1 any marker / 3 usage — a natural sibling to `lint-guard-pins.sh` (both are meta-lints over repo hygiene).
`[ev: PR2 incidental find (niagara-research 8b47fed4 / probe 2026-09-07-c11-pr2-second-read.md); C8-archive incident]`

### S4 — S23 fixtures lack a comment-only decoy (SC-10)
The cross-cutting rule (SC-10) requires every fixture to carry a comment-only decoy that does NOT satisfy the pin. The
C10 S23 (silent-protection Pattern-B) fixtures (`tests/lint-silent-protection.bats` S23-pos / S23-and) were flagged in the
C10 verify phase as lacking one. **C12**: add a comment-only decoy (a WARN/trip/`newOffnormalAlarm` token inside a `//`
or `/* */` comment) to each S23 fixture and re-confirm it does not flip the verdict. `[ev: C10 verify warning (per investigador); lint-silent-protection.bats S23 fixtures @ 07d2fc7]`

## Resolved in C11 — NOT C12 seeds (recorded so C12 does not re-open them)
- **B832-G2** (silent-protection Case-B raw-line `//`-only strip, missing `/* */`) — **RESOLVED by T1**: the shared
  `mb_strip` strips `//` AND `/* */` (I5), and silent-protection now runs `mb_parse` on the pre-stripped lines
  (`lint-silent-protection.sh:313`). The fragment header records it: "I5 `/* */` strip in mb_strip alongside `//`
  (B832-G2; no biting fixture; pinned by 3×3 baselines)". `[ev: method-boundary.sh:13/:29 @ 07d2fc7]`
- **B832-G1** (one-line accessor skip) — RESOLVED by T1, pinned by G-accessor / C11-g1-setter.
- **The K24(7) pin-attribution rule** is now MECHANISED by `lint-guard-pins.sh` (T4): every `lint-*.sh` must declare a
  `# Mutation: <id> -- …` resolving to a real `@test`. The recurring "OBSERVED mutation names the wrong fixture" pattern
  (caught by hand in C10 R-lessons, C11 PR1 I2/I3, C11 PR2 task 2.7) is now a lint. `[ev: lint-guard-pins.sh @ 07d2fc7]`
- **"A QA pin must never hardcode a fixture line"** — applied in C11 PR4 (T4-smoke now COMPUTES the S21-misparse line).
  A close-lesson, not a seed.

## Self-verify
| # | Seed/note | Marker | Anchor |
|---|-----------|--------|--------|
| S1 | @-stop param-annotation FN | [CERT] | method-boundary.sh:84; reproduced |
| S2 | I3 initializer reachability | [CERT] | method-boundary.sh:74/:89; reproduced |
| S3 | sweep-markers.sh reusable lint | [ev] | PR2 incidental + C8-archive |
| S4 | S23 comment-only decoy gap | [ev] | C10 verify warning; S23 fixtures |
| — | B832-G2/G1 resolved by T1; K24(7) mechanised | [CERT] | method-boundary.sh; lint-guard-pins.sh |
Tally: 2 [CERT] · 2 [ev-only] · resolved-notes [CERT].
